#Application properties
spring:
	application:
		name:api-gateway
server:
	port:9095

# Configuration properties
spring:
	cloud:
		config:
			uri:http://localhost:9092,http://localhost:9093
			failFast:true

